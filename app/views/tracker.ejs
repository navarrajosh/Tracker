<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <nav class="navMenu">
        <a  class="nav" href="/login">Login</a>
    </nav>
</head>
<body>
<section id="#HOME" style="height: 100vh;">
    <div class="container">

        <div class="row height d-flex justify-content-center align-items-center">

          <div class="col-md-6">
            <form action="/users" method="get">
              
            <div class="formdropdown">
              <div class="search-dropdown">
              <div class="dropdown-content" id="dropdownContent"></div>
                  <select id="searchFilter" name="selectedOption">
                    <option >Filter</option>
                    <option value="assetatag">Asset Tag</option>
                    <option value="username">Username</option>
                    <option value="eid">Employee ID</option>
                  </select>
              </div>
              <input type="text" id="searchInput" name="q" class="form-control form-input" placeholder="Search..." >
              <button type="submit">Search</button>
            </div>
            </form>
          </div>  
        </div> 
      </div>
</section>
<section id="#sResults">
  <h1 id="searchResults"> </h1>
</section>
<script> 
    // const searchInput = document.getElementById('searchInput');
    // const filtercontent = document.getElementById('searchFilter');

    // searchInput.addEventListener('input', () => {
    //   const searchTerm = searchInput.value.trim();

    //   if (searchTerm === '') {
    //     dropdownContent.innerHTML = ''; // Clear dropdown when the input is empty
    //   } else {
    //     // Fetch suggested users from the server based on the search term
    //     fetch(`/users?q=${searchTerm}`)
    //       .then(response => response.json())
    //       .then(data => {
    //         // Display the dropdown with suggestions
    //         dropdownContent.innerHTML = '';
    //         data.forEach(user => {
    //           const suggestionElement = document.createElement('a');
    //           suggestionElement.textContent = user.username;
    //           suggestionElement.href = `/users/${user.eid}`; // Replace with the link to the user's profile or details page
    //           dropdownContent.appendChild(suggestionElement);
    //         });
    //       })
    //       .catch(error => console.error('Error fetching suggested users:', error));
    //   }
    // });

    // // Hide the dropdown when clicking outside of it
    // document.addEventListener('click', event => {
    //   if (!event.target.closest('.search-dropdown')) {
    //     dropdownContent.innerHTML = '';
    //   }
    // });
</script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>
</body>
</html>