<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
</head>
<body>
    <h1>Data from Database</h1>
    <ul>
      <% data.forEach(item => { %>
        <p><%= item.eid %> | <%= item.account %> | <%= item.contact %> | <% item.site %> | <% item.username %></p>
      <% }); %>
    </ul>
</body>
</html>